package br.aplicacao.eletrica.calculo;

public class balancoDeCorrentes {
	/*
	 * public balancoDeCorrentes(ArrayList<Object> entidade) {
	 * 
	 * ArrayList<Integer> cargaSeq = new ArrayList<Integer>();
	 * 
	 * ArrayList<Double> FFF = new ArrayList<Double>(); ArrayList<Double> FF = new
	 * ArrayList<Double>(); ArrayList<Double> FN = new ArrayList<Double>();
	 * 
	 * for (int i = 0; i < entidade.size(); i++) {
	 * 
	 * double corrente = 0; int nFases = 0;
	 * 
	 * if (entidade.get(i) instanceof Equipamento) { corrente = ((Equipamento)
	 * entidade.get(i)).getPotenciaEmVA(); } else { corrente = ((Circuito)
	 * entidade.get(i)).getCorrenteIB(); }
	 * 
	 * if (entidade.get(0) instanceof Carga) { nFases = ((Carga)
	 * entidade.get(i)).getnFases(); } else {
	 * 
	 * nFases = ((Circuito) entidade.get(i)).getNumFases();
	 * 
	 * } switch (nFases) {
	 * 
	 * case 1: FN.add(corrente); cargaSeq.add(1); break; case 2: FF.add(corrente);
	 * cargaSeq.add(2); break; case 3: FFF.add(corrente); cargaSeq.add(3); break;
	 * 
	 * } } }
	 */
}
